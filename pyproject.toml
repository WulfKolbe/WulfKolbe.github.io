[project]
name = "wiki-tools"
version = "0.1.0"
description = "Scripts that generate TiddlyWiki .tid files from other formats"
requires-python = ">=3.12"
dependencies = [
    "bibtexparser~=1.4",
]

[tool.uv]
package = false
